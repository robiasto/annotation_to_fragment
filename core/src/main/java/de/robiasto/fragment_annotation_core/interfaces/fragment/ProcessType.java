package de.robiasto.fragment_annotation_core.interfaces.fragment;

/**
 * Enum class representing the different types of processes.
 * The ProcessType enum has three constant values: ANY, TYPE, and ANNOTATION.
 * These values are used to determine the type of processing to be performed.
 */
public enum ProcessType {
    ANY,
    TYPE,
    ANNOTATION
}
